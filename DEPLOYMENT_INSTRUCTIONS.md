# Инструкции по развертыванию новой логики Bot Flow Designer

## 🚀 ЧТО РЕАЛИЗОВАНО:

### ✅ Кодовая часть:
1. **Новые обработчики кнопок** (`bot/handlers/flow_buttons.py`)
2. **Интеграция в главный обработчик** (`bot/handlers/main_handler.py`)
3. **Поддержка callback_query** для inline кнопок
4. **Динамические кнопки** на основе баланса пользователя
5. **Новые плейсхолдеры** для Address Label API данных

### ✅ Шаблоны (готовы к добавлению в админку):
1. **`deep_analysis_example`** - пример глубокого анализа
2. **`payment_methods`** - методы оплаты
3. **Обновленный `free_check_result`** - с новыми плейсхолдерами

## 📋 ЧТО НУЖНО СДЕЛАТЬ В АДМИНКЕ:

### 1. Создать новые шаблоны:

#### Шаблон "deep_analysis_example":
- **Ключ:** `deep_analysis_example`
- **Русский текст:** (см. файл `NEW_BOT_FLOW_TEMPLATES.md`)
- **Английский текст:** (см. файл `NEW_BOT_FLOW_TEMPLATES.md`)

#### Шаблон "payment_methods":
- **Ключ:** `payment_methods`
- **Русский текст:** (см. файл `NEW_BOT_FLOW_TEMPLATES.md`)
- **Английский текст:** (см. файл `NEW_BOT_FLOW_TEMPLATES.md`)

### 2. Обновить существующий шаблон:
- **Ключ:** `free_check_result`
- **Обновить** с новыми плейсхолдерами: `{label}`, `{address_type}`, `{website}`, `{address_status}`

## 🔧 РАЗВЕРТЫВАНИЕ:

### 1. Закоммитить изменения:
```bash
git add .
git commit -m "Добавлена новая логика Bot Flow Designer с динамическими кнопками"
git push origin clean-main
```

### 2. Развернуть на Heroku:
```bash
git push heroku clean-main:main --force
```

### 3. Проверить логи:
```bash
heroku logs --app checkyourcrypto-bot --tail
```

## 🧪 ТЕСТИРОВАНИЕ:

### 1. Тест бесплатной проверки:
- Отправить адрес в бот
- Проверить, что показывается результат с Address Label API данными
- Проверить динамические кнопки (зависят от баланса)

### 2. Тест кнопки "Показать пример":
- Нажать "📊 Показать пример"
- Проверить, что показывается пример глубокого анализа
- Проверить кнопки (зависят от баланса)

### 3. Тест кнопки "Пополнить баланс":
- Нажать "💰 Пополнить баланс"
- Проверить, что показываются методы оплаты
- Проверить кнопки оплаты

### 4. Тест кнопки "Назад":
- Нажать "🔙 Назад"
- Проверить, что возвращается к результату проверки
- Проверить, что кнопки снова динамические

## 🎯 ЛОГИКА РАБОТЫ:

### Динамические кнопки:
- **При достаточном балансе:** "🛡️ Глубокий AI-анализ", "📊 Показать пример", "🏠 Главное меню"
- **При недостаточном балансе:** "📊 Показать пример", "💰 Пополнить баланс", "🏠 Главное меню"

### Обработка кнопок:
- **"📊 Показать пример"** → показывает пример анализа
- **"💰 Пополнить баланс"** → показывает методы оплаты
- **"🔙 Назад"** → возврат к результату проверки
- **"🛡️ Заказать AI-анализ"** → проверяет баланс и показывает статус

## 🚨 ВАЖНЫЕ ЗАМЕЧАНИЯ:

1. **Шаблоны нужно добавить в админке** - без них кнопки будут показывать ошибки
2. **Плейсхолдеры автоматически заменяются** - `{balance}`, `{paid_check_price}`, `{label}`, etc.
3. **Callback_query обрабатываются автоматически** - через webhook
4. **Логика баланса уже работает** - проверяется в коде

## 🔄 СЛЕДУЮЩИЕ ШАГИ:

1. ✅ Добавить шаблоны в админке
2. ✅ Развернуть код на Heroku
3. ✅ Протестировать функциональность
4. 🔄 Реализовать платную проверку (сейчас показывает заглушку)
5. 🔄 Интегрировать Binance Pay
6. 🔄 Добавить личный кабинет

## 📞 ПОДДЕРЖКА:

При возникновении проблем:
1. Проверить логи Heroku
2. Убедиться, что шаблоны добавлены в админке
3. Проверить, что callback_query обрабатываются
4. Проверить баланс пользователя в базе данных
